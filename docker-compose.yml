version: "3.9"

services:
  api:
    build: .
    container_name: dns-api
    ports:
      - "8000:8000"
    environment:
      APP_NAME: DNS Management API
      APP_ENV: development
    volumes:
      - ./app:/app/app
    restart: unless-stopped
